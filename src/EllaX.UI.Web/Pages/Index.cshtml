@page "/"
@using EllaX.Core.Dtos
@using Humanizer
@inject HttpClient Http

<h1>Hello, world!</h1>

@if (_healthDtos == null)
{
    <p>
        <em>Loading...</em>
    </p>
}
else
{
    <table class="table">
        <thead>
        <tr>
            <th>Latitude</th>
            <th>Longitude</th>
            <th>Last Seen</th>
        </tr>
        </thead>
        <tbody>
        @foreach (HealthDto healthDto in _healthDtos)
        {
            <tr>
                <td>@healthDto.Latitude</td>
                <td>@healthDto.Longitude</td>
                <td>@DateTimeOffset.UtcNow.AddMinutes(-healthDto.Age).Humanize()</td>
            </tr>
        }
        </tbody>
    </table>
}

<div id="mapid"></div>

@functions {
    HealthDto[] _healthDtos;

    protected override async Task OnInitAsync()
    {
        _healthDtos = await Http.GetJsonAsync<HealthDto[]>("http://localhost:5000/api/network/health");
    }

}
