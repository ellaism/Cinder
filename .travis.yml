dist: xenial
addons:
  snaps:
  - name: dotnet-sdk
    classic: true
    channel: latest/beta
sudo: required
language: csharp
mono: none
solution: Cinder.sln
before_install:
  - sudo snap alias dotnet-sdk.dotnet dotnet
install:
  - dotnet restore
script:
  - dotnet build
